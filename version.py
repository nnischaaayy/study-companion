# version.py — Single source of truth for app version
# Update this before every release, everything else reads from here

__version__ = "1.0.0"
GITHUB_OWNER = "nnischaaayy"   # ← replace with your GitHub username
GITHUB_REPO  = "study-companion"        # ← replace with your repo name
